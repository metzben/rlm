# Copy to .env (gitignored) and set your own registry namespace.
# The sandbox VM pulls the image itself, so it must be pushed somewhere
# the VM can reach (e.g. your Docker Hub account).
REGISTRY=docker.io/<your-dockerhub-namespace>
